# Sample VIKTOR Apps

This folder contains two small VIKTOR apps that can be used with the workflow agent template.

- `reaction_loads`: generates random support reactions from a distributed load.
- `footing`: sizes a rectangular pad footing from reaction rows.

Each app is standalone and includes its own `app.py`, `requirements.txt`, and `viktor.config.toml`.
